# Host-side Python dependencies (the Pico runs MicroPython — no pip there).
# Install with:  pip install -r requirements.txt
# Versions pinned to the environment used to produce the report (Python 3.9).
numpy==2.0.2
scipy==1.13.1
matplotlib==3.9.4
flask==3.1.3

# Only needed by the superseded scripts in legacy/ (not the active pipeline):
pillow==11.3.0
reportlab==4.5.1
