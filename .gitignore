# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Per-session recordings (auto-created by host/server.py)
logs/

# WiFi credentials (user-specific, never commit)
pico/wifi_config.py

# Work-in-progress, not ready to publish
interpretability/
notes/
# P-wave analysis: held back until reviewed. Remove a line to publish that script.
host/analyze_p_wave.py
host/compare_p_groups.py
host/compare_p_wave.py
host/compare_p_windows.py
host/quantify_shoulder.py
host/show_pwave_panels.py

# Ad-hoc recordings in repo root (not the curated samples/)
/ecg_*.csv
/ecg_*.png
/peaks_*.csv
/markers_*.csv

# Reports: ignore auto-generated outputs (HTML/docx, LaTeX build files) but TRACK
# the hand-written LaTeX source, the figures/tables needed to compile it, and the
# final compiled PDF (the main deliverable).
reports/*
!reports/holter_report.tex
!reports/holter_report.pdf
!reports/tables.tex
!reports/figs/
!reports/figs_manual/
